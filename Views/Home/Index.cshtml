@{
    ViewData["Title"] = "Home Page";
}
<div class="container">
    <div class="row"> 
        <div class="col-sm-4">
            <h1>Login</h1> 
            @Html.Partial("./Partials/LoginForm")
        <hr>
            <h1>Registration</h1>                  
            @Html.Partial("./Partials/RegisterForm")
        </div>
    </div>
</div>
